# Known source discrepancies

Use the session topic, document-wide context, and corroborating audio. Do not route from one stray number or footer.

- `4. Odwaga.pdf` labels its content as session #5, while `5. Postawa Fizjologiczna.pdf` labels its content as session #4. The skill follows the internal program labels: S04 physiology and S05 courage.
- `6. Produlktywnosc.pdf` has a session #6 header, but some internal lines and worksheets say session #5. Route the whole document to S06 Productivity.
- `7. Mindset.pdf` has a session #7 header, while an internal section says session #6. Route it to S07 Mindset.
- `8. Wpływ.pdf`, page 5, is mislabeled as session #3 Energy even though the page continues the influence model's role-model component. Keep it under S08 Influence.
- `2.2. karta_jasnosci.pdf` is image-based and yielded no extracted text. Its three pages were visually inspected; do not treat an empty text extract as an empty source.
- `Merytoryczne podstawy związane z energią-audio.mp4` and the file with suffix `(1)` have identical SHA-256 content. Only one is transcribed and cited.
- Instructor recording titles sometimes begin with an exercise number that is not the program session number. Route by recording content, not the first numeral.

When new evidence contradicts this list, preserve both readings and lower confidence rather than silently choosing one.
